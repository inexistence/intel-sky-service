# Completion audit

This is the requirement-to-evidence audit for the 22 completion conditions in the project goal.
`PROVEN` means the current source plus the cited automated or attended runtime evidence covers the
condition.

The protocol and behavioral details remain authoritative in `ProtocolCatalog.md` and
`OfficialBehaviorNotes.md`; this file indexes evidence rather than duplicating those ledgers.

| # | Completion condition | Status | Authoritative evidence |
| --- | --- | --- | --- |
| 1 | Complete visual Computer Use Runtime protocol directory | PROVEN | `ProtocolCatalog.md` partitions all 34 ARM request descriptors into 15 implemented and 19 `OUT_OF_SCOPE` requests, catalogs asynchronous records and the 12-selector Intel PIP XPC ABI; `Tools/reverse/protocol-catalog.test.mjs` checks the partition against source and ARM metadata. |
| 2 | Stable Window/AX state | PROVEN | `AccessibilitySnapshotTests`, `AccessibilityTreeDifferTests`, `ElementSnapshotCacheTests`, `WindowScreenshotterTests`; official Notes runs returned complete 21,780+ character AX trees and screenshots across repeated refresh and service restarts. |
| 3 | All public Computer Use actions stable | PROVEN | The eleven public methods are indexed in `README.md`; action payloads and stale-target/policy/intervention gates are covered by `MacAppActionPerformerTests`, `MacAccessibilityActionsTests`, `MacKeyboardInputTests`, and `MacScrollInputTests`. Attended client runs cover click, scroll, type, and paste without foreground activation. |
| 4 | App discovery, policy, start, stop, modify, frontmost window | PROVEN | All request rows are `IMPLEMENTED` in `ProtocolCatalog.md`; routing/schema/lifecycle coverage is in `SkyProtocolTests`, `MacAppPolicyTests`, `MacAppResolverTests`, `WorkspaceAppCatalogTests`, and `ComputerUseSessionCoordinatorTests`. Official client runs cover discovery, policy, auto-launch, and state. |
| 5 | Truly asynchronous Capture Stream | PROVEN | `AppCaptureSessionTests` covers native-change delivery, long polling, independent concurrent requests, bounded completion/failure, deadlines, owner disconnect, PID replacement, and turn races; the protocol rows expose the confirmed start/update envelopes. |
| 6 | Event Stream Start / Status / Stop | PROVEN | `EventStreamSessionTests` covers persistent status/files, idempotence, direct input records, privacy filtering, lock, disconnect, matching turn end, and explicit stop; `SkyProtocolTests` verifies all three routes and schemas. |
| 7 | Coordinated focus, cursor, intervention, lock, turn state machines | PROVEN | `ComputerUseTurnLifecycleTests` verifies serialized safety ordering; `ComputerUseFocusCoordinatorTests`, `SystemFocusStealGuardTests`, `VirtualCursorCoordinatorTests`, `ScreenLockCheckerTests`, and session/capture/event/PIP lifecycle tests cover each fan-out and race. |
| 8 | Physical input interrupts and latches requires-requery | PROVEN | `AppInteractionTrackerTests`, `MacAppActionPerformerTests`, and `SystemFocusStealGuardTests` cover attributed and unknown physical input, in-flight cancellation, per-App latching, fresh-state recovery, and fail-open focus protection. Installed status reports physical Input Monitoring available. |
| 9 | Lock, App stop/exit, PID/window replacement, disconnect cleanup | PROVEN | Lock/stop/disconnect cleanup is covered by `ScreenLockCheckerTests`, `ComputerUseSessionCoordinatorTests`, `AppCaptureSessionTests`, and PIP tests. Attended Calculator PID 19587→19637 retained one presentation and completed host context replacement; attended Finder window replacement and repeated service disconnects are recorded in `OfficialBehaviorNotes.md`. |
| 10 | Turn-ended terminates capture, event, focus, cursor, presentation | PROVEN | Deterministic coverage exists in `ComputerUseTurnLifecycleTests`, `AppCaptureSessionTests`, `EventStreamSessionTests`, `ComputerUseFocusCoordinatorTests`, `VirtualCursorCoordinatorTests`, and `RemoteHostedPIPPresentationCoordinatorTests`. Production Intel ChatGPT does not send the explicit request; its native host retires the presentation after the official 30-second grace. In an attended deployed run, two presentation-scoped code-3 responses stopped SCStream and finalized the video queue without a subsequent Computer Use request. Explicit request and observed turn-transition paths remain supported. |
| 11 | Official ChatGPT Remote Hosted PIP shows stable live target content | PROVEN | Attended Notes/Finder/Calculator runs use the unmodified Intel `sky.node` host, authenticated Apple Event bootstrap, anonymous XPC endpoint, CAContext/IOSurface frames, and official presentation logs. Repeated frames and action refreshes were visible on glass. |
| 12 | PIP is not gray, black, or placeholder content | PROVEN | Attended full Notes and Calculator frames are recorded after the IOSurface correction, including three service reconnects; returned screenshots and host PIP both show complete target content. Historical gray/crop diagnoses are retained in `OfficialBehaviorNotes.md`. |
| 13 | PIP displays and updates the virtual cursor | PROVEN | Attended Notes scroll/click showed the ARM SoftwareCursor inside PIP with correct upright orientation and no external cursor panel; asymmetric pixel, geometry, pressed-state, and delayed-callback tests cover transport and lifecycle. |
| 14 | PIP supports resize, window replacement, turn-ended | PROVEN | Finder normal↔Info window resize/replacement and Calculator PID context replacement are attended `CONFIRMED_INTEL_RUNTIME`; unit tests cover host resize, context replacement, turn end, host-retirement liveness, disconnect, initial-publish race, and reconnect race. The attended Notes lifecycle run stopped and deallocated capture three seconds after the official host retired the completed presentation, without needing another request. |
| 15 | Computer Use does not move the physical pointer | PROVEN | Every physical fallback uses process-targeted `CGEvent.postToPid`; AX page scroll is tested not to move the pointer. PIP/local cursor rendering is visual-only and input-transparent; no action path posts through the global HID tap. |
| 16 | Normal operation does not steal foreground focus | PROVEN | Attended background TextEdit type/paste completed without foreground activation; process-targeted actions and synthetic focus leases are covered by `ComputerUseFocusCoordinatorTests` and action tests. |
| 17 | Real user focus changes are not forcibly reversed | PROVEN | `userFocusChangePreventsTurnEndRestore`, `physicalInputPreventsTurnEndRestore`, safety termination, and focus-guard fail-open tests prove restoration suppression after independent user activity. |
| 18 | Stable service restart and automatic reconnect | PROVEN | An authenticated socketless watchdog publishes ChatGPT's recovered official status envelope; three consecutive TERM/reconnect cycles returned full Notes state in 1.339 s, 1.145 s, and 1.186 s through one persistent official client, each with first-attempt PIP bootstrap. |
| 19 | No duplicate stale service or multiple socket owners | PROVEN | Runtime process/socket audits after deployment and every reconnect show one ChatGPT-managed service, one socketless watchdog child, and one mode-0600 socket owner. Socket inode/PID-safe cleanup has dedicated lifecycle tests. |
| 20 | Core Computer Use remains usable during temporary PIP failure | PROVEN | PIP publication failures are isolated from request results by construction and tests. A production LaunchServices fallback returned a full Notes state while the native host was unavailable; pending presentations later republish on authenticated host reconnect. |
| 21 | Tests, runtime evidence, ledgers, and remaining differences delivered | PROVEN | Final checkpoint: 269 Swift tests and 13 Node protocol/reverse/oracle tests pass. The x86_64 warnings-as-errors release is signed with the required Apple Development identity and installed at the managed path with SHA-256 `16ef59e1bb7e60fdcdb92886ef7ea635681dd22af16f4362f73c4f0348bb4575`; the managed controller restarted it as PID 8578 with capability and permission health fields true. The release passed the attended production host-retirement test and an attended Appshot composer comparison confirming the ARM card structure and dimensions. Runtime gates show one ChatGPT-managed service, one socketless watchdog, one mode-0600 socket owner, and no video-queue activity after teardown. `OfficialBehaviorNotes.md` retains all `PARTIAL`, `KNOWN_DIFFERENCE`, and `NEEDS_ARM_ORACLE` items; `ProtocolCatalog.md` is machine checked. |
| 22 | Audio, Skysight, Messages explicitly out of scope | PROVEN | `ProtocolCatalog.md` lists exactly 19 `OUT_OF_SCOPE` request types for Audio, Skysight, and Messages; source and protocol tests prove safe rejection before provider dispatch or side effects. |

## Remaining differences and oracle work

All known calibration gaps remain explicitly labeled in `OfficialBehaviorNotes.md` and in the
request-level evidence column of `ProtocolCatalog.md`. The executable `Tools/oracle` case matrix
covers the eleven public Window APIs and low-level `startApp`; the `Oracle backlog` section records
the additional attended ARM cases for action timing, ViewBridge focus behavior, lock/input
intervention, capture backpressure, and exact error mapping. These do not silently expand Intel's
permissions or substitute invented behavior: uncertain cases fail closed or retain the documented
conservative implementation.
